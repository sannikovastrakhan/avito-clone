# День 3: Условия в Python на примере объявлений

print("=" * 60)
print("УСЛОВИЯ (IF/ELIF/ELSE)")
print("=" * 60)

# Пример 1: Простое условие
print("\n--- ПРИМЕР 1: Проверка цены ---")
price = 50000

if price > 100000:
    print("💰 Дорогой товар")
elif price > 20000:
    print("💵 Средний ценовой сегмент")
elif price > 5000:
    print("💸 Бюджетный товар")
else:
    print("🆓 Почти даром")

print(f"Цена: {price} ₽")

# Пример 2: Несколько условий (and, or)
print("\n--- ПРИМЕР 2: Сложные условия ---")
ad = {
    "title": "MacBook Pro",
    "price": 120000,
    "category": "Электроника",
    "city": "Москва",
    "is_active": True,
    "views": 150,
    "has_photo": True
}

# Проверяем, хорошее ли объявление
if ad["price"] < 50000 and ad["has_photo"]:
    print("🔥 Отличная цена и есть фото!")
elif ad["price"] < 100000 and ad["views"] > 100:
    print("👀 Популярное объявление в среднем ценовом сегменте")
elif ad["category"] == "Электроника" and ad["city"] == "Москва":
    print("📱 Электроника в Москве")
else:
    print("📌 Обычное объявление")

# Пример 3: Тернарный оператор (условие в одну строку)
print("\n--- ПРИМЕР 3: Короткая запись условия ---")
price = 75000
delivery = "Бесплатно" if price > 50000 else "Платная доставка"
print(f"Цена: {price} ₽, {delivery}")

# Пример 4: Проверка наличия ключа в словаре
print("\n--- ПРИМЕР 4: Проверка наличия поля ---")
if "phone" in ad:
    print(f"Телефон указан: {ad['phone']}")
else:
    print("📞 Телефон не указан (ключ 'phone' отсутствует)")

# Пример 5: Множественные условия для фильтрации
print("\n--- ПРИМЕР 5: Фильтр для объявлений ---")
def check_ad_quality(ad):
    """Функция оценивает качество объявления"""
    
    score = 0
    reasons = []
    
    # Проверяем цену
    if ad["price"] > 0:
        score += 1
    else:
        reasons.append("Цена не указана")
    
    # Проверяем фото
    if ad.get("has_photo", False):
        score += 2
    else:
        reasons.append("Нет фотографий")
    
    # Проверяем просмотры
    if ad["views"] > 50:
        score += 1
        reasons.append("Популярное")
    
    # Проверяем категорию
    if ad["category"] in ["Электроника", "Недвижимость"]:
        score += 1
        reasons.append("Популярная категория")
    
    # Итоговая оценка
    if score >= 4:
        quality = "⭐ Отличное объявление"
    elif score >= 2:
        quality = "👍 Хорошее объявление"
    else:
        quality = "👎 Требует доработки"
    
    return quality, reasons

# Проверяем наше объявление
quality, reasons = check_ad_quality(ad)
print(f"Оценка: {quality}")
if reasons:
    print("Особенности:", ", ".join(reasons))

print("\n✅ Условия изучены!")
# День 3: Условия в Python на примере объявлений

print("=" * 60)
print("УСЛОВИЯ (IF/ELIF/ELSE)")
print("=" * 60)

# Пример 1: Простое условие
print("\n--- ПРИМЕР 1: Проверка цены ---")
price = 50000

if price > 100000:
    print("💰 Дорогой товар")
elif price > 20000:
    print("💵 Средний ценовой сегмент")
elif price > 5000:
    print("💸 Бюджетный товар")
else:
    print("🆓 Почти даром")

print(f"Цена: {price} ₽")

# Пример 2: Несколько условий (and, or)
print("\n--- ПРИМЕР 2: Сложные условия ---")
ad = {
    "title": "MacBook Pro",
    "price": 120000,
    "category": "Электроника",
    "city": "Москва",
    "is_active": True,
    "views": 150,
    "has_photo": True
}

# Проверяем, хорошее ли объявление
if ad["price"] < 50000 and ad["has_photo"]:
    print("🔥 Отличная цена и есть фото!")
elif ad["price"] < 100000 and ad["views"] > 100:
    print("👀 Популярное объявление в среднем ценовом сегменте")
elif ad["category"] == "Электроника" and ad["city"] == "Москва":
    print("📱 Электроника в Москве")
else:
    print("📌 Обычное объявление")

# Пример 3: Тернарный оператор (условие в одну строку)
print("\n--- ПРИМЕР 3: Короткая запись условия ---")
price = 75000
delivery = "Бесплатно" if price > 50000 else "Платная доставка"
print(f"Цена: {price} ₽, {delivery}")

# Пример 4: Проверка наличия ключа в словаре
print("\n--- ПРИМЕР 4: Проверка наличия поля ---")
if "phone" in ad:
    print(f"Телефон указан: {ad['phone']}")
else:
    print("📞 Телефон не указан (ключ 'phone' отсутствует)")

# Пример 5: Множественные условия для фильтрации
print("\n--- ПРИМЕР 5: Фильтр для объявлений ---")
def check_ad_quality(ad):
    """Функция оценивает качество объявления"""
    
    score = 0
    reasons = []
    
    # Проверяем цену
    if ad["price"] > 0:
        score += 1
    else:
        reasons.append("Цена не указана")
    
    # Проверяем фото
    if ad.get("has_photo", False):
        score += 2
    else:
        reasons.append("Нет фотографий")
    
    # Проверяем просмотры
    if ad["views"] > 50:
        score += 1
        reasons.append("Популярное")
    
    # Проверяем категорию
    if ad["category"] in ["Электроника", "Недвижимость"]:
        score += 1
        reasons.append("Популярная категория")
    
    # Итоговая оценка
    if score >= 4:
        quality = "⭐ Отличное объявление"
    elif score >= 2:
        quality = "👍 Хорошее объявление"
    else:
        quality = "👎 Требует доработки"
    
    return quality, reasons

# Проверяем наше объявление
quality, reasons = check_ad_quality(ad)
print(f"Оценка: {quality}")
if reasons:
    print("Особенности:", ", ".join(reasons))
print("\n✅ Условия изучены!")
print("\n--- МОЯ ПРАКТИКА ---")
# Создайте своё объявление и напишите для него условия:

my_ad = {
    "title": "Велосипед горный",
    "price": 25000,
    "category": "Спорт",
    "city": "СПб",
    "has_photo": True,
    "views": 30
}

# 1. Проверьте, подходит ли объявление для показа на главной:
#    - Цена от 10000 до 50000 ИЛИ категория "Спорт"
#    - И обязательно наличие фото
#
# 2. Если подходит — выведите "Можно на главную!", иначе "Обычное объявление"

# Напишите код здесь:

# Проверяем условие для главной страницы
if (10000 < my_ad["price"] < 50000 and my_ad["has_photo"]) or (my_ad["category"] == "Спорт" and my_ad["has_photo"]):
    print("✅ Можно на главную!")
else:
    print("📌 Обычное объявление")

# Добавим ещё проверку — почему именно не подходит
print("\nДетальная проверка:")
if not (10000 < my_ad["price"] < 50000):
    print("  - Цена вне диапазона 10000-50000")
if not my_ad["has_photo"]:
    print("  - Нет фотографий")
if my_ad["category"] != "Спорт":
    print("  - Не категория Спорт")
